resources:
  jobs:
    demo_job:
      name: "[${bundle.target}] DAB CI-CD Test Job"

      tasks:
        - task_key: run_demo

          spark_python_task:
            python_file: ../src/demo.py

          environment_key: default

      environments:
        - environment_key: default

          spec:
            client: "1"